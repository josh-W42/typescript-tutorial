/*
  A short program that runs over why types are important
  TLDR: Types are everywhere and they can help immensely
  with autocompletion and error checking.
*/

const today = new Date();
today.getDate();

const person = {
  age: 20,
};
person.age;
person.happy;

class Color {}
const red = new Color();
